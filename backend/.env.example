PORT=5000

# Base de données MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=votre_mot_de_passe_mysql
DB_NAME=gestion_stages_memoires

# JWT — choisissez une clé longue et aléatoire
JWT_SECRET=votre_secret_jwt_tres_long_et_aleatoire
JWT_EXPIRES_IN=7d

# Email (optionnel — pour les notifications futures)
EMAIL_USER=votre@email.com
EMAIL_PASS=mot_de_passe_application
